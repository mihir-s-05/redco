{
  "schema_version": 1,
  "status": "frozen_before_amended_live_observation",
  "amends": "configs/stage-b/rlm-structural-trace-preregistration.json",
  "reason": "The first launch reached a healthy inference server but the control command resolved the editable task environment outside the pinned verifiers project because --project was invoked from the repository root. EvalConfig validation failed before environment construction, so no rollout or gate observation occurred.",
  "observed_failed_attempt": {
    "stage_reached": "control configuration validation",
    "rollouts_started": 0,
    "traces_recorded": 0,
    "error": "EvalConfig rejected env after the editable overlay resolved the wrong verifiers package context"
  },
  "single_execution_change": {
    "field": "control command working directory",
    "old": "Run uv from the repository root with --project pointing to the patched verifiers clone.",
    "new": "Change into the patched verifiers clone before uv run, matching the previously validated native audit launch path.",
    "cpu_dry_run": "passed before amendment freeze"
  },
  "unchanged": [
    "model and frozen model state",
    "task prompt and forced-recursion protocol",
    "sampling seed and temperature",
    "completion and total-token limits",
    "RLM and verifiers commits and patch hashes",
    "all mandatory checks and exact thresholds",
    "resource price and runtime caps",
    "artifact and termination policy"
  ],
  "interpretation": {
    "pass": "The trace linkage blocker is cleared and a representative recorded-rollout branch-pair RAF experiment may be preregistered.",
    "fail": "Stop and repair structural capture or import; do not run representative branch-pair RAF measurement."
  }
}
