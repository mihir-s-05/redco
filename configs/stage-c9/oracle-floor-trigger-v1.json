{
  "schema_version": 1,
  "status": "frozen_before_any_oracle-floor-update",
  "diagnostic": "Stage C9 oracle-advantage parameter-sharing floor",
  "default": "do_not_run",
  "run_if_either": [
    "A final project writeup will make a practical claim that local node labeling improves on matched-data branch-global labeling.",
    "A Stage D design decision depends on selecting local node labeling over matched-data branch-global labeling before Stage D supplies that comparison."
  ],
  "skip_if": [
    "The final writeup limits the local increment to the Stage C7 mechanistic diagnostics.",
    "Stage D fields both local and branch-global arms, so the real-task campaign itself resolves the labeling increment."
  ],
  "required_if_triggered": [
    "new preregistration before any 4B LoRA update",
    "frozen Stage C9 collected batches",
    "oracle advantages exactly zero at redundant-context digit nodes",
    "12 matched optimizer updates from the same frozen initialization",
    "exact constrained categorical rescoring of causal gain and nuisance drift",
    "separate reporting from the original Stage C9 frozen decision"
  ],
  "interpretation": "This diagnostic estimates shared-network nuisance drift under perfect labels. It is not CPU-free because faithful parameter coupling requires real 4B LoRA updates."
}
