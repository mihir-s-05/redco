[project]
name = "redco-evidence-selection-v2"
version = "0.1.0"
description = "Deterministic exact-evidence QASPER taskset for ReDCO Stage D0."
requires-python = ">=3.12"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["redco_evidence_selection_v2"]
