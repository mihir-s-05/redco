diff --git a/verifiers/v1/interception/server.py b/verifiers/v1/interception/server.py
index 19ede9a..608c991 100644
--- a/verifiers/v1/interception/server.py
+++ b/verifiers/v1/interception/server.py
@@ -524,7 +524,7 @@ class InterceptionServer(Interception):
                         upstream_request = dialect.apply_overrides(
                             body, session.ctx.model, directed_sampling
                         )
-                        call_response = await session.ctx.client.get_response(
+                        request_operation = session.ctx.client.get_response(
                             dialect,
                             body,
                             session.ctx.model,
@@ -536,6 +536,16 @@ class InterceptionServer(Interception):
                             observer_context=observer_context,
                             observer=session.observer,
                         )
+                        if (
+                            session.observer is not None
+                            and structure is not None
+                            and structure.depth > 0
+                        ):
+                            call_response = await session.observer.run_concurrent_children(
+                                request_operation
+                            )
+                        else:
+                            call_response = await request_operation
                         logger.debug(
                             "intercept turn: id=%s tools=%d",
                             session.trace.id,
